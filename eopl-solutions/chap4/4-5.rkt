;; (value-of exp₁ p σ₀) = (val₁ σ₁)
;; ...
;; (value-of exp_n p σ_n-1) = (val_n σ_n)
;; ---------------------------------------
;; (value-of (list-exp exp₁ ... exp_n) p σ₀)
;; = (list-val val₁ ... val_n)
